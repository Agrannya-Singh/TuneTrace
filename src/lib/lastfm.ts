// This file is no longer used, but kept for reference if needed.
// The primary song interface is now in /src/lib/spotify.ts
export interface Song {
  id: string;
  title: string;
  artist: string;
  albumArtUrl: string;
}
